<div align="center">

# Taix

**An open-source, cross-platform terminal session manager with AI integration**

[![CI](https://github.com/rynvaro/taix/actions/workflows/ci.yml/badge.svg)](https://github.com/rynvaro/taix/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Tauri](https://img.shields.io/badge/Tauri-2.x-24c8d8?logo=tauri)](https://tauri.app)
[![Rust](https://img.shields.io/badge/Rust-1.80+-orange?logo=rust)](https://www.rust-lang.org)

![Taix Screenshot](docs/assets/screenshot.png)

</div>

---

## What is Taix?

Taix is a **lightweight, native GUI application** for managing multiple terminal sessions — think iTerm2 or Windows Terminal, but cross-platform and with deep AI integration built in from the ground up.

**Core philosophy:** The terminal is the most direct interface between a human and a machine. AI should integrate naturally into that interface, not live in a separate window.

**Target users:** DevOps engineers, backend developers, system administrators.

### Key Features (Phase 1 — MVP)

- **Multi-tab terminal** — open multiple shell sessions, switch between them without losing state
- **Cross-platform PTY** — native PTY on macOS/Linux (POSIX), ConPTY on Windows
- **Keep-alive tabs** — switching tabs never destroys the xterm.js instance; long-running processes (`top`, `vim`, etc.) keep running
- **Persistent configuration** — appearance, shell, and AI settings saved to TOML on disk
- **Settings UI** — font, theme (dark/light/system), default shell, all editable in-app
- **Type-safe IPC** — Rust ↔ TypeScript bindings auto-generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta)

### Planned Features (Phase 2 & 3)

- **Split panes** — horizontal/vertical terminal splits
- **SSH profiles** — connect to remote machines with saved credentials
- **Session persistence** — save and restore named sessions across restarts (SQLite)
- **AI assist** — natural language → command (`#` trigger), error analysis, command suggestions
- **Ollama support** — fully local AI, no API key required
- **AI Agent mode** — AI executes commands with per-action confirmation and full audit log

---

## Tech Stack

| Layer | Technology |
|---|---|
| GUI framework | [Tauri 2](https://tauri.app) |
| Terminal rendering | [xterm.js 5](https://xtermjs.org) |
| Frontend | React 19 + TypeScript 5 + Tailwind CSS 4 |
| Backend | Rust (stable) |
| PTY | [portable-pty](https://crates.io/crates/portable-pty) |
| State management | [Zustand](https://github.com/pmndrs/zustand) |
| Config storage | TOML via [toml-rs](https://crates.io/crates/toml) |
| Type generation | [tauri-specta](https://github.com/oscartbeaumont/tauri-specta) |

---

## Getting Started

### Prerequisites

| Tool | Version | Install |
|---|---|---|
| Rust | ≥ 1.80 | `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh` |
| Node.js | ≥ 20 | [nodejs.org](https://nodejs.org) |
| npm | ≥ 9 | bundled with Node.js |

**Linux only** — install WebKit and GTK system dependencies:
```bash
sudo apt-get install -y libwebkit2gtk-4.1-dev libssl-dev libgtk-3-dev \
  libayatana-appindicator3-dev librsvg2-dev
```

### Run in development

```bash
git clone https://github.com/rynvaro/taix.git
cd taix
npm install
npm run tauri dev
```

### Build for production

```bash
npm run tauri build
```

Outputs are placed in `src-tauri/target/release/bundle/`:
- **macOS** → `.dmg`
- **Linux** → `.AppImage` + `.deb`
- **Windows** → `.exe` (NSIS installer)

---

## Project Structure

```
taix/
├── src/                        # Frontend (React + TypeScript)
│   ├── components/
│   │   ├── layout/             # AppLayout, StatusBar
│   │   ├── terminal/           # TerminalTab, TerminalPane, TabBar
│   │   ├── session/            # SessionList
│   │   └── settings/           # SettingsModal, AppearanceSettings, ShellSettings
│   ├── hooks/                  # useTerminal, useResize
│   ├── services/               # pty.ts, config.ts  (Tauri IPC wrappers)
│   ├── stores/                 # sessionStore, uiStore, settingsStore (Zustand)
│   └── types/
│       └── bindings.ts         # Auto-generated Rust ↔ TS types
│
├── src-tauri/                  # Backend (Rust)
│   └── src/
│       ├── commands/           # pty.rs, config.rs  (Tauri IPC handlers)
│       ├── config/             # schema.rs, mod.rs  (TOML config)
│       ├── pty/                # session.rs, manager.rs, platform.rs
│       ├── error.rs            # Unified AppError
│       ├── state.rs            # AppState (injected into commands)
│       └── lib.rs              # App entry, specta export
│
├── .github/workflows/
│   ├── ci.yml                  # Test on Linux + macOS + Windows
│   └── release.yml             # Build & publish on v* tags
│
└── docs/
    ├── planning.md             # Project planning & PTY deep-dive
    ├── architecture.md         # Code architecture & design decisions
    └── phase1-tasks.md         # Phase 1 task breakdown & progress
```

---

## Development

### Available scripts

```bash
npm run tauri dev      # Start development server + native window
npm run tauri build    # Build production bundle
npm run type-check     # TypeScript type check (tsc --noEmit)
npm run lint           # ESLint
npm run lint:fix       # ESLint with auto-fix
```

### Rust tests

```bash
cd src-tauri
cargo test             # Run all unit + integration tests
```

### Regenerate TypeScript bindings

TypeScript bindings (`src/types/bindings.ts`) are auto-generated from Rust types on every debug build or test run:

```bash
cd src-tauri && cargo test bindings_contain_expected_symbols
```

---

## CI / CD

Every push and PR runs three parallel jobs on **Linux, macOS, and Windows**:
- Rust unit + integration tests (`cargo test --all`)
- TypeScript type-check + ESLint

Releases are triggered by pushing a `v*` tag and automatically produce installers for all three platforms via [tauri-action](https://github.com/tauri-apps/tauri-action).

---

## Roadmap

| Phase | Status | Highlights |
|---|---|---|
| Phase 1 — MVP | ✅ Complete | Multi-tab PTY, settings UI, config persistence, CI/CD |
| Phase 2 — Session management | 🔲 Planned | Split panes, SSH, session persistence (SQLite), search |
| Phase 3 — AI integration | 🔲 Planned | Command suggestions, error analysis, Ollama, Agent mode |

---

## Keyboard Shortcuts

| Shortcut | Action |
|---|---|
| `⌘T` | New tab (opens New Session modal) |
| `⌘W` | Close active pane / session |
| `⌘1`–`⌘9` | Switch to Nth tab |
| `⌘[` | Focus previous pane |
| `⌘]` | Focus next pane |
| `⌘D` | Split active pane horizontally |
| `⌘⇧D` | Split active pane vertically |
| `⌘F` | Open in-terminal search |
| `Escape` | Close search / modal |

> On Linux / Windows replace `⌘` with `Ctrl`.

---

## Contributing

Contributions are welcome! Please open an issue before starting large changes so we can discuss the approach.

1. Fork the repo and create a feature branch
2. Make your changes and ensure `cargo test` and `npm run type-check` pass
3. Submit a pull request against `dev`

---

## License

[MIT](LICENSE) © 2026 Taix Contributors
